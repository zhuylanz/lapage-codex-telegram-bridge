# 2026-06-19

## Context
- Started building a local Telegram-to-Codex CLI bridge.

## Decisions
- Project path follows the user's requested spelling: `/Users/huylan/Code/lapage-codex-telegram-bride`.

## Notes
- The bridge should run Codex in a persistent PTY and relay Telegram messages into stdin.
- Implemented with `grammy`, macOS `script(1)`, `dotenv`, and `strip-ansi`.
- `npm run typecheck`, `npm run build`, and `npm audit --audit-level=moderate` pass.
- Telegram auth check passed for `@groot_agent_bot`; startup smoke test stayed up for 8 seconds.
- `node-pty` was removed because it failed locally with `posix_spawnp failed` on macOS arm64 / Node v24.
- Replaced `script(1)` with tmux after Telegram showed `script: tcgetattr/ioctl: Operation not supported on socket`.
- tmux backend uses `send-keys` for input and `pipe-pane` plus temp log polling for output.
- `npm run typecheck`, `npm run build`, tmux smoke test, and `npm run dev` smoke test pass after tmux change.

## Follow-Ups
- Run `npm run dev` and test `/status` in Telegram; attach with `tmux attach -t codex-telegram-bridge` for local debugging.

## 2026-06-20 Update
- Telegram screenshot showed prompts were pasted into Codex but not submitted.
- Changed input delivery to `tmux set-buffer` + `paste-buffer` and default submit key `C-j`.
- Added `CODEX_SUBMIT_KEY` and `CODEX_STARTUP_DELAY_MS` config.
- Follow-up testing showed `C-j` did not submit. Working sequence is paste, wait 800ms, then tmux `Enter`.
- Added `CODEX_SUBMIT_DELAY_MS=800`; real Codex tmux test returned `ENV_SUBMIT_OK`.
- Refactored monolithic `src/index.ts` into `config`, `codex-session`, `telegram-bridge`, `text`, and `tmux` modules.
- Switched output relay from raw `pipe-pane` stream to cleaned `capture-pane` snapshots to remove TUI artifacts.
- Further restricted output relay to only send latest completed Codex bullet response; suppresses `Working (... esc to interrupt)` repeated snapshots.
- Fixed parser to suppress only explicit progress lines; normal response text containing “working” now relays correctly.
- Added Telegram streaming by editing a single message; parser streams latest response block while excluding `• Working (...)` progress bullet.
